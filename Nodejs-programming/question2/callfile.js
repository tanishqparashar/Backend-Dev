const stringutil = require("./stringutil");

console.log(stringutil.capitalize("hello"));      // Hello
console.log(stringutil.reverseString("hello"));   // dlrow
console.log(stringutil.countVowels("hello"));// 3
